---
description: Acknowledge drinking water — resets the timer.
---

Invoke Bash with exactly this command (no `cd`, no chaining, no redirection): `node __HOOK_PATH__ --ack`

The CLI is silent on success. Reply with one short warm line — e.g. "Timer reset — nice." — and move on.
